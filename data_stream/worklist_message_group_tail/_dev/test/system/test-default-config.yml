input: cel
service: worklist_message_group_tail
vars: ~
data_stream:
  vars:
    url: "http://{{Hostname}}:{{Port}}"
    enable_request_tracer: true
    page_size: 1
    initial_interval: 24h
    api_key: xxxx
    preserve_original_event: true
assert:
  hit_count: 4
