input: cel
service: request_body_id_timestamp_filter
vars: ~
data_stream:
  vars:
    url: "http://{{Hostname}}:{{Port}}/test/api"
    enable_request_tracer: true
    batch_size: 2
    sub_path: booking/history
    initial_interval: 24h
    user: amiuser
    password: amipassword
    preserve_original_event: true
assert:
  hit_count: 6
