input: cel
service: url_query_basic_auth_manual_next_link
vars: ~
data_stream:
  vars:
    url: "http://{{Hostname}}:{{Port}}/test/api"
    enable_request_tracer: true
    batch_size: 2
    sub_path: booking
    username: amiusername
    password: amipassword
    preserve_original_event: true
assert:
  hit_count: 6
