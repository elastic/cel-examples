input: cel
service: url_query_oauth2_next_links
vars: ~
data_stream:
  vars:
    url: "http://{{Hostname}}:{{Port}}/test/api"
    enable_request_tracer: true
    batch_size: 2
    sub_path: booking
    client_id: amiuser
    client_secret: amipassword
    token_url: "http://{{Hostname}}:{{Port}}/testoauth/token"
    grant_type: client_credentials
    preserve_original_event: true
assert:
  hit_count: 6
